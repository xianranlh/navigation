# 星穹导航 - 个人导航页

一款崩坏：星穹铁道风格的个人导航页，帮助你快速访问常用网站。

## ✨ 特性

- 🎨 **崩铁主题** - 深空紫配色、毛玻璃效果、星光粒子动画
- ⚡ **一键添加** - 粘贴 URL 自动获取标题、描述、图标
- 🔍 **智能搜索** - 即时搜索，支持 `/` 快捷键聚焦
- 🖱️ **拖拽排序** - 自由拖拽链接和分类
- 📱 **响应式** - 完美支持桌面和移动端
- 🌙 **主题切换** - 支持亮色/暗色模式

## 🚀 快速开始

### 本地开发

```bash
# 1. 启动 MySQL 数据库
docker-compose up -d mysql

# 2. 后端
cd backend
npm install
npm run db:push     # 同步数据库
npm run db:seed     # 填充示例数据
npm run dev

# 3. 前端
cd frontend
npm install
npm run dev
```

访问 http://localhost:3000

### Docker 部署

```bash
docker-compose up -d
```

访问 http://localhost

## ⌨️ 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl + K` | 聚焦搜索框 |
| `Ctrl + D` | 快速添加链接 |
| `/` | 搜索 |
| `Esc` | 关闭弹窗 |

## 📁 项目结构

```
navigation/
├── frontend/          # Vue 3 前端
│   ├── src/
│   │   ├── components/    # 组件
│   │   ├── views/         # 页面
│   │   ├── stores/        # Pinia 状态管理
│   │   └── api/           # API 接口
│   └── ...
├── backend/           # Express 后端
│   ├── src/
│   │   ├── controllers/   # 控制器
│   │   └── routes/        # 路由
│   └── prisma/            # 数据库模型
└── docker-compose.yml
```

## 🛠️ 技术栈

- **前端**: Vue 3, Vite, Pinia, Vue Router
- **后端**: Node.js, Express, Prisma
- **数据库**: MySQL 8.0
- **部署**: Docker, Nginx

## 📄 License

MIT
