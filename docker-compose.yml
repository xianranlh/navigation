services:
  nav:
    image: nav:latest
    build: .
    container_name: nav
    ports:
      - "13000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:/app/data/dev.db
    volumes:
      - data:/app/data
    restart: unless-stopped

volumes:
  data:
